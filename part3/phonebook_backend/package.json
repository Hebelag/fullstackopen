{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "description": "Backend for phonebook from part2",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf dist && npm install && cd ../../part2/phonebook && npm install && npm run build && cp -r dist ../../part3/phonebook_backend",
    "build:ui_win": "@powershell Remove-Item -Recurse -Force dist && cd ../../part2/phonebook && npm run build && @powershell Copy-Item dist -Recurse ../../part3/phonebook_backend",
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:full": "npm run build:ui && npm start",
    "lint": "eslint ."
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "mongodb": "^7.1.0",
    "mongoose": "^9.2.4",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "eslint": "^10.0.3",
    "globals": "^17.4.0"
  }
}
